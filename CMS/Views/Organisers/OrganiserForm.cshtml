@model CMS.ViewModels.OrganiserFormViewModel

@{
    ViewBag.Title = "New";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div class="createOne">
    <h2>New Oragniser</h2>

    @using (Html.BeginForm("Save", "Organisers"))
    {
        @Html.ValidationSummary(true, "Please fix the following errors.")
        <div class="form-group">
            @Html.LabelFor(m => m.Organiser.DisplayName, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
            @Html.TextBoxFor(m => m.Organiser.DisplayName, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.Organiser.DisplayName)
            </div>
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.Organiser.ContactDetails, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
            @Html.TextBoxFor(m => m.Organiser.ContactDetails, new { @class = "form-control" })
            @Html.ValidationMessageFor(m => m.Organiser.ContactDetails)
                </div>
        </div>
        @Html.HiddenFor(m => m.Organiser.OrganiserId)
        @Html.AntiForgeryToken()
        <button type="submit" class="btn btn-primary">Save</button>
    }
    </div>
    @section scripts
{
        @Scripts.Render("~/bundles/jqueryval")
    }
